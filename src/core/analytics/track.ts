// src/core/analytics/track.ts
// Placeholder para analytics. Mantenerlo desacoplado desde el inicio.
export function track(event: string, props?: Record<string, any>) {
   
  console.log("[track]", event, props ?? {});
}
